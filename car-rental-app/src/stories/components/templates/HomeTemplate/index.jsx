import React from "react";
import CardListSection from "../../organisms/CardListSection";

const HomeTemplate = ({ cars }) => {
  return <CardListSection cars={cars} />;
};

export default HomeTemplate;
